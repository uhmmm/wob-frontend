import React from 'react'
import styled from '@emotion/styled'

const PaginationContainer = styled.div({})

const Pagination = () => <PaginationContainer></PaginationContainer>

export { Pagination }
